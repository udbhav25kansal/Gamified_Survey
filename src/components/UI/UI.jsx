import React from 'react';

const UI = () => {
    return (
        <div className="ui-overlay">
            {/* Optional: Can add survey progress, stats, or other UI elements here later */}
        </div>
    );
};

export default UI;
